{
  "name": "newtab01",
  "private": true,
  "version": "1.0.19",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "package": "rm -rf dist && tsc && vite build && mkdir -p build && (cd dist && zip -r ../build/newtab01-$(node -p \"require('../package.json').version\").zip . -x '*.DS_Store')",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@crxjs/vite-plugin": "^2.6.1",
    "fuse.js": "^7.4.2"
  }
}
